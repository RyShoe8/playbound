// Superseded by verify-maintenance.ts, which imports the catalog data directly
// instead of parsing source with regexes. Safe to delete this file.
console.error("Use `npm run verify:maintenance` (scripts/verify-maintenance.ts).");
process.exit(1);
